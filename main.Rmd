---
title: "Bayesian Information Borrowing with Applications in Clinical Trials"
output: bookdown::html_document2
bibliography: reference.bib
date: "2025-10-22"
---
<style type="text/css">
  body {
    line-height: 1.5; /* Sets line height for the entire body */
  }
  p {
    text-indent: 2em;
  }
</style>

<style>
  figcaption, p.caption {
    text-align: left;
    text-indent: 0;
  }
</style>

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE, message = FALSE, warning = FALSE, fig.width = 7, fig.height = 6, fig.align='center')
library(rstan)
library(ggplot2)
library(dplyr)
library(knitr)
library(kableExtra)
library(purrr)
source("Utils.R")
```

# Introduction {#sec1}

本文旨在完善Bayesian统计在临床试验中应用时涉及的算法，所考虑的先验仅包括混合先验和幂先验。章节\@ref(sec2)简介混合先验及幂先验信息借用的统计学理论。章节\@ref(sec3)讨论混合先验和幂先验信息借用在SAP模板的各主要终点中的应用 (对应的频率学派方法为MMRM、ANCOVA、negative binomial regression、stratified CMH以及stratified Cox-PH regression)。章节\@ref(sec4)将基于`rstan`实现混合先验及幂先验信息借用在实际数据中的应用，通过不同场景下的模拟案例，就参数估计和假设检验，比较不同类型的先验下，频率学派方法、混合先验信息借用和幂先验信息借用的统计性能。

# Method and Notation {#sec2}

令$D$表示分析数据，$D_{hst}$表示历史数据，$\theta$表示模型参数，$w\in [0, 1]$表示混合先验的权重参数或幂先验的温度参数。令$D=(Y, A, Z)$分别表示分析数据的结局、治疗分组和协变量，$D_{hst}=(Y_{hst}, A_{hst}, Z_{hst})$分别表示历史数据的结局、治疗分组和协变量，$1\leq i\leq n$表示第i个个体。$p_{hst}(\theta|D_{hst})$表示信息先验。

## 混合先验信息借用

混合先验将信息先验$p_{hst}(\theta|D_{hst})$和模糊先验$\pi_{vag}(\theta)$按一定的权重混合，其中，$\pi_{vag}(\theta)$通常取弱信息共轭先验（如对二分类终点，可取Jeffreys' prior；对其他终点可取unit information prior）[@Kass1995; @Schmidli2014]，
\begin{equation}
p_{mp}(\theta|w) \propto wp_{hst}(\theta|D_{hst})+(1-w)\pi_{vag}(\theta). (\#eq:1)
\end{equation}

若无法确定$w$的值，可将$w$也看作随机变量，此时将由数据自适应决定权重分配，然而，将$w$看作随机变量并不等同于dynamically borrowing [@Yang2023]，
\begin{equation}
p_{mp}(\theta,w)\propto\left(wp_{hst}(\theta|D_{hst})+(1-w)\pi_{vag}(\theta)\right)\pi(w). (\#eq:2)
\end{equation}

除了式\@ref(eq:1)和\@ref(eq:2)的两种方法以外，还有 @Yang2023 提出的动态信息借用的混合先验self-adapting mixture prior (SAM prior)，同时，他们在文章中指出混合先验\@ref(eq:2)的统计性能并不好：*In addition, we investigate three alternative approaches: (a) a fully Bayesian approach by assigning $w$ a uniform prior ... none of these approaches perform well.* 此外，也有相关的材料指出$w\sim Unif(0, 1)$时fully Bayesian approach的推断结果与$w=0.5$相近。 

## 幂先验信息借用

幂先验的表达式如下：
$$p_{pp}(\theta|w) \propto L(\theta|D_{hst})^w\pi_0(\theta).$$
从表达式不难得出，当$w=0$时，幂先验不借用历史数据$D_{hst}$；当 $w=1$时，幂先验完全借用历史数据$D_{hst}$。幂先验的一个重要性质为当$\pi_0(\theta)$为proper prior时，幂先验也是proper prior [@Ibrahim2015]。

同样地，若无法确定$w$的值，幂先验也可将$w$也看作随机变量，此时有
\begin{equation}
p_{pp}(\theta,w)\propto L(\theta|D_{hst})^w\pi_0(\theta)\pi(w).(\#eq:3)
\end{equation}

然而，与混合先验不同的是，若不对信息先验$L(\theta|D_{hst})^w\pi_0(\theta)$标准化，该幂先验会违背Likelihood Principle [@Robert2007; @Berry2010]：当历史数据相同时，对历史数据取不同的充分统计量，得到的先验分布也会不同，因而会产生不同的推断结果。因此，在将$w$看作随机变量时，需要进行如下标准化，
\begin{equation}
p_{npp}(\theta,w)=p_{pp}(\theta|w)\pi(w)=\frac{L(\theta|D_{hst})^w\pi_0(\theta)}{\int L(\theta|D_{hst})^w\pi_0(\theta)d\theta}\pi(w).(\#eq:4)
\end{equation}
经过标准化的幂先验\@ref(eq:4)又称为NPP (Normalized power prior) 或MPP (Modified power prior)[@Ibrahim2015; @Banbeta2019]。对于initial prior $\pi_0(\theta)$的选择，从后验的收敛角度考虑，建议$\pi_0(\theta)$取proper prior。

NPP中的标准化项$C(w)=\int L(\theta|D_{hst})^w\pi_0(\theta)d\theta$通常没有显式表达式，因此常使用积分近似，具体的算法主要包括以下几类：

* Laplace approximation
* Monte Carlo Metropolis-Hastings (MCMH) algorithm [@Liang2013]
* Path sampling [@Friel2008; @Ye2022]

由于$w$取值较小时，使用Laplace approximation可能会有较大偏差，本文档仅介绍后两种方法。使用MH算法从NPP相应的后验分布中抽样时，主要困难在于rejection的步骤中，比值$C(w)/C(w^\prime)$无法计算。基于MCMH算法，一种可行的方法是在MH算法中嵌入以下近似：
$$\frac{C(w)}{C(w^\prime)}=\frac{\int L(\theta|D_{hst})^w\pi_0(\theta)d\theta}{\int L(\theta|D_{hst})^{w^\prime}\pi_0(\theta)d\theta}=\int\frac{L(\theta|D_{hst})^w\pi_0(\theta)}{L(\theta|D_{hst})^{w^\prime}\pi_0(\theta)}\cdot\frac{L(\theta|D_{hst})^{w^\prime}\pi_0(\theta)}{\int L(\theta|D_{hst})^{w^\prime}\pi_0(\theta)d\theta}d\theta=\lim_{M\to\infty}\frac{1}{M}\sum_{i=1}^M\frac{L(\theta_i|D_{hst})^w}{L(\theta_i|D_{hst})^{w^\prime}}, a.s.,$$
其中$\theta_i\sim p_{pp}(\theta|w^\prime)\propto L(\theta|D_{hst})^{w^\prime}\pi_0(\theta)$。因此，我们可以通过MCMC算法抽取足够多的$\theta_i$来计算$C(w)/C(w^\prime)$。

Path sampling算法主要基于 @Friel2008 的如下结果

$$\log C(w)=\int_0^wE_{p_{pp}(\theta|w^\ast)}[l(\theta|D_{hst})]dw^\ast,$$
其中$p_{pp}(\theta|w^\ast)\propto L(\theta|D_{hst})^{w^\ast}\pi_0(\theta)$。因此，通过先在$[0, 1]$区间划分$K$个网格，对于每一个网格端点$w_k$，使用MCMC算法从$p_{pp}(\theta|w_k)$分布对$\theta$抽样，可以近似$E_{p_{pp}(\theta|w_k)}[l(\theta|D_{hst})]$，最后基于网格和近似得到的期望可以近似计算$\log C(w)$。具体算法如下 [@Ye2022]：

* Step 0: Choose a set of $K−1$ different numbers as knots between 0 and 1, and another knot at 1, with $K$ sufficiently large. Sort them in ascending order $(w_1, . . . , w_{K−1}, 1)$. Let $w_0 = 0$, $\Delta_1 = w_1, \Delta_i = w_i−w_{i−1} (1 < i ≤ K),\text{ and }\Delta_K = 1−w_{K−1}$. Choose $M$, the number of MCMC samples in a run when sampling from $p_{pp}(\theta|w)$. Initialize $l = 1$.
* Step 1: Generate $M$ samples from $p_{pp}(\theta|w_l)$ using an appropriate MCMC algorithm. Denote the sample as $(\theta_l^{(1)},\theta_l^{(2)},\dots,\theta_l^{(M)})$.
* Step 2: Calculate $h(w_l) = \sum^M_{j=1}l(\theta_l^{(j)}|D_{hst})/M$.
* Step 3: Calculate $\log C(w_l) ≈ \sum^l_{k=1} \Delta_k\{h(w_{k−1}) + h(w_k)\}$ (the Trapezoidal rule).
* Step 4: Increase $l$ by 1. If $l \leq n$ then repeat Steps 1 to 3.

相较于MCMH算法，path sampling在`stan`中易于实现。此外，MCMH算法在每次从proposal distribution抽样时，都要再嵌套一层MCMC抽样来计算acceptance ratio；而path sampling算法可以独立于MH算法给出所有的$\log C(w_l)$，因此计算效率远高于MCMH算法。综合考虑，尽管MCMH算法可能更加精准和稳定，本文优先使用path sampling实现NPP方法。在\@ref(sec4)中，由于$\log C(w)$在ANCOVA模型下有显式表达式，本文将比较使用精确$\log C(w)$和使用path sampling近似时的区别。

@Neuenschwander2009 指出unnormalized power prior \@ref(eq:3)在某些情况下不适用（如在估计rate时对$w$的选择会趋于0）。@Hobbs2011 指出NPP没有直接将历史数据和新数据间的可比性参数化，并且无论是unnormalized power prior还是NPP，往往都倾向于衰减$w$：*A problem with modified joint power priors is that they do not directly parameterize the commensurability of the historical and new data... Furthermore, Neelon and O’Malley (2010) caution against using Ibrahim–Chen and MPPs because they both tend to overattenuate the impact of the historical data, forcing the use of fairly large $\alpha_0$ (or in our case, fairly informative hyperpriors for $\alpha_0$) to deliver sufficient borrowing. * 同时，他们也给出了新的幂先验方法commensurate power prior (CPP)来解决上述问题。本文暂不考虑CPP方法的应用。

## Remark

无论是混合先验，还是幂先验，需要明确研究中的目标参数。若冗余参数取值在历史数据和分析数据中相近，此时将冗余参数也纳入信息先验中，会导致在随机权重的模型中，赋予历史数据的权重膨胀，从而增加目标参数的估计偏差。因此，一般不建议将冗余参数也纳入信息先验，而是像 @Hobbs2011 文中通过极大似然估计等替代历史数据中的冗余参数。

# Application {#sec3}

<!-- ## 非肿瘤项目 -->

## 连续变量终点

<!-- #### MMRM模型 -->

### ANCOVA模型

ANCOVA的模型假设如下：
$$Y_i=\alpha+\tau A_i+Z_i^T\gamma+\varepsilon_i, \varepsilon_i\sim N(0, \sigma^2).$$
也即，
$$Y\sim N(X\beta, \sigma^2I),$$
其中$X=[1, A, Z]\in\mathbb{R}^{n\times p}$，$\beta = [\alpha, \tau, \gamma^T]^T$。

对于历史数据，有$\beta\sim N(\hat{\beta},\sigma^2(X^TX)^{-1})$，其中$\sigma^2$可用无偏估计$\hat{\sigma}^2=RSS/(n-p)$代入。因此，混合先验为
$$p_{mp}(\beta|w)\sim wN(\hat{\beta},\hat\sigma^2(X^TX)^{-1})+(1-w)\pi_{vag}(\beta).$$

对于$\pi_{vag}(\beta)$的选取，可用unit information prior $\pi_{vag}(\beta)=N(\hat\beta_{hst},n\hat\sigma^2(X_{hst}^TX_{hst})^{-1})$。


幂先验为 [@Ibrahim2003]
$$
p_{pp}(\beta,\sigma^2|w)\propto (\sigma^2)^{-\frac{nw}{2}}\exp\left\{-\frac{1}{2}(\sigma^2/w)^{-1}(Y_{hst}-X_{hst}\beta)^T(Y_{hst}-X_{hst}\beta)\right\}\pi_0(\theta).
$$
同样使用无偏估计代入$\sigma^2$，此时幂先验可表示为
$$p_{pp}(\beta|w)\propto N\left(\hat\beta_{hst},w^{-1}\hat\sigma^2(X_{hst}^TX_{hst})^{-1}\right)\pi_0(\beta).$$
从该表达式也可看出，当温度$w$增加时，将借用更多的历史信息，而当温度$w$降低时，幂先验的密度将会趋于平坦，从而趋近无信息先验。在后续的模拟研究中，出于对后验收敛性的考虑，取幂先验的initial prior为
$$\pi_0(\beta)=\pi_{vag}(\beta)=N(\hat\beta_{hst},n\hat\sigma^2(X_{hst}^TX_{hst})^{-1}).$$

此时，若考虑$w$为随机变量，则
\begin{equation}
\log C(w)=const - \frac{pw}{2}\log 2\pi-\frac{w}{2}\log |\hat\sigma^2(X_{hst}^TX_{hst})^{-1})|-\frac{p}{2}\log w+\log N(\hat\beta_{hst};\hat\beta_{hst},(w^{-1}+n)\hat\sigma^2(X_{hst}^TX_{hst})^{-1}),(\#eq:5)
\end{equation}
可以直接计算，在接下来的章节将用于评估path sampling算法的精确度。

<!-- ## 肿瘤项目 -->

# Simulation Study {#sec4}

<!-- ## 非肿瘤项目 -->

## 连续变量终点

对于连续变量终点，为简化问题，设定如下数据生成过程：

$$Y_{i}-Y_i^{baseline}=\alpha+(\tau+\delta I(i\in D))A_{i}+\gamma Y_{i}^{baseline}+\varepsilon_{i},\\
Y_i^{baseline}\overset{i.i.d.}{\sim}N(0, \sigma_Y^2),\\
\varepsilon_{i}\overset{i.i.d.}{\sim}N(0, \sigma^2).$$

其中，参数$\tau$为历史数据的治疗效应，$\beta=\tau+\delta$为当前研究的治疗效应，$|\delta|$衡量当前研究的数据与历史数据间的data conflict大小。当前研究的样本量$n=56$，历史数据的样本量$n_{hst}=100$，治疗组和对照组的比例为$1:1$。对于参数，取$\alpha=0.5, \tau = 0.6, \gamma = 0.1, \sigma_Y = 0.5$，$\sigma$的选取满足$Var(Y_i)=1$，此时对应ANCOVA的$R^2=0.13$，在单侧$\alpha=0.025$时检验$\tau>0$的power约为0.8。模拟研究的第一部分以0.05为步长将$\delta$从-0.6递增至0来评估prior-data conflict下各方法的表现。

``` {r echo=TRUE}
# --- Sample size estimation ---
library(pwrss)
gamma <- 0.1
sd_baseline <- 0.5
power.t.regression(beta = 0.6, sd.predictor = 0.5, sd.outcome = sqrt(1+gamma^2*sd_baseline^2-(1+gamma)^2*sd_baseline^2), k.total = 2, power = 0.8, alpha = 0.025, alternative = "one.sided")
```

此外，为了探索试验设计初期中，如何从统计模拟的角度给出合理的$w$，还将分别变化混合先验和幂先验的$w$，来观察type I error和power随$w$的变化情况。在模拟中同时记录deviance information criterion (DIC)来探索从信息准则角度使用DIC确定$w$的可能性 [@Spiegelhalter2002; @Ibrahim2012; @Ibrahim2015]。模拟主要考虑两种场景：

* 场景一：type I error随$w$的变化，历史数据治疗效应$\tau=0.6$，当前研究无治疗效应；
* 场景二：power随$w$的变化，历史数据和当前研究治疗效应均为$\tau=0.6$，但入组困难，power仅能达到0.7；
* 场景三：

场景二对应的样本量为

``` {r echo=TRUE}
# --- Sample size estimation ---
library(pwrss)
gamma <- 0.1
sd_baseline <- 0.5
power.t.regression(beta = 0.6, sd.predictor = 0.5, sd.outcome = sqrt(1+gamma^2*sd_baseline^2-(1+gamma)^2*sd_baseline^2), k.total = 2, power = 0.7, alpha = 0.025, alternative = "one.sided")
```

最后，为了探索数据分析阶段，如何基于先验参数$w$进行敏感性分析，将基于strong & moderate prior-data conflict场景（$\delta = -0.6 \text{ and } -0.3$）生成模拟数据，通过改变先验参数$w$完成tipping-point analysis。

<!-- #### MMRM模型 -->

### ANCOVA模型

ANCOVA模型的模拟研究考虑以下几种信息先验方法：

1. Fixed mixture prior (FMP) with $w=0.5$ and $\pi_{vag}(\beta)=N(\hat\beta_{hst},n\hat\sigma^2(X_{hst}^TX_{hst})^{-1})$;
2. Random mixture prior (RMP) with $w\sim Beta(1, 1)$ and $\pi_{vag}(\beta)=N(\hat\beta_{hst},n\hat\sigma^2(X_{hst}^TX_{hst})^{-1})$;
3. Fixed power prior (FPP) with $w=0.5$ and $\pi_0(\beta)=\pi_{vag}(\beta)$;
4. Unnormalized power prior (UPP) with $w\sim Beta(1, 1)$ and $\pi_0(\beta)=\pi_{vag}(\beta)$;
5. Normalized power prior (ANPP) with $w\sim Beta(1, 1)$ and $\pi_0(\beta)=\pi_{vag}(\beta)$ using path sampling;
6. Normalized power prior (NPP) with $w\sim Beta(1, 1)$ and $\pi_0(\beta)=\pi_{vag}(\beta)$ using exact $\log C(w)$ based on \@ref(eq:5);
7. ANCOVA with current data.

在模拟研究中，对于点估计和区间估计，比较bias、RMSE、credible/confidence interval (CI) length和coverage probability (CP)；对于假设检验，比较type I error和power，其中Bayes方法在$P(\beta>0|D,D_{hst})>0.975$时拒绝原假设（等价于$\beta$的95%可信区间下界大于0）。

#### 试运行结果展示

以下给出ANPP和NPP方法在一组示例模拟数据上的试运行结果（$\delta=0$，也即没有prior-data conflict），其中`beta[3]`为治疗效应，`sigma2`为残差方差$\sigma^2$，`w_eff`为权重或温度。图\@ref(fig:fig4-1)和\@ref(fig:fig4-2)展示试运行结果的MCMC算法收敛情况（trace plot通过比较各个chain的抽样分布来检查收敛性，pair plot中若出现黄色和红色的散点则需要注意抽样的收敛性，在一些hierarchical models可能会出现以上问题，此时应通过non-centered parameterization处理模型）：

```{r include=FALSE}
# # --- Example run ---
n <- 156
n_cur <- 56
n_hst <- n - n_cur
alpha <- 0.5
tau <- 0.6
gamma <- 0.1

# Historical data generation
# set.seed(123)
# treatment_hst <- rep(c(1, 0), each = n_hst/2)
# hba1c_baseline_hst <- rnorm(n_hst)
# hba1c_baseline_hst <- scale(hba1c_baseline_hst)*0.5 + 45
# X <- model.matrix(~ hba1c_baseline_hst + treatment_hst)
# z <- rnorm(n_hst)
# e_hst <- resid(lm(z ~ hba1c_baseline_hst + treatment_hst))
# e_hst <- e_hst/sd(e_hst)*sqrt(1 - tau^2*0.25 - (1+gamma)^2*0.25)
# hba1c_hst <- hba1c_baseline_hst + alpha + tau*treatment_hst + gamma*hba1c_baseline_hst + e_hst
# data_hst <- data.frame(hba1c_baseline = hba1c_baseline_hst,
#                        treatment = treatment_hst,
#                        hba1c = hba1c_hst)
# # # ancova_ps_model <- build_ancova_path_model()  # Build path sampling model
# # # save(ancova_ps_model, file = "ancova_ps.RData")
# load("ancova_ps.RData")
# options(mc.cores = 8)
# knots <- ancova_path_integrate(ancova_ps_model, data_hst, I(hba1c - hba1c_baseline) ~ hba1c_baseline + treatment)
# save("knots", "data_hst", file = "Historical.RData")

load("Historical.RData")

## Current data generation
set.seed(123)
delta <- 0
treatment <- rep(c(1, 0), each = n_cur/2)
hba1c_baseline <- rnorm(n_cur)
hba1c_baseline <- scale(hba1c_baseline)*0.5 + 45
e <- rnorm(n_cur, sd = sqrt(1 - tau^2*0.25 - (1+gamma)^2*0.25))
hba1c <- hba1c_baseline + alpha + (tau + delta)*treatment + gamma*hba1c_baseline + e
data_cur <- data.frame(hba1c_baseline, treatment, hba1c)

## Extract results
tabulize <- function(stan_fits = list(),
                     lm_fits = list(),
                     model_names = c(),
                     treatment_pars = c(),
                     effect_hst,
                     effect_cur,
                     caption = "") {
  n_mdls <- length(model_names)
  mean <- rep(NA, n_mdls)
  lb <- rep(NA, n_mdls)
  ub <- rep(NA, n_mdls)
  
  n_w <- nrow(summary(stan_fits[[1]]$fit, "w_eff")$summary)
  meanw <- matrix(NA, nrow = n_mdls, ncol = n_w)
  lbw <- matrix(NA, nrow = n_mdls, ncol = n_w)
  ubw <- matrix(NA, nrow = n_mdls, ncol = n_w)
  
  for (i in 1:length(stan_fits)) {
    idx <- match(treatment_pars, stan_fits[[i]]$data$cols)
    res <- summary(stan_fits[[i]]$fit, pars = "beta")$summary[idx, ]
    mean[i] <- res["mean"]
    lb[i] <- res["2.5%"]
    ub[i] <- res["97.5%"]
    res <- summary(stan_fits[[i]]$fit, pars = "w_eff")$summary
    meanw[i, ] <- res[, "mean"]
    lbw[i, ] <- res[, "2.5%"]
    ubw[i, ] <- res[, "97.5%"]
  }
  for (j in 1:length(lm_fits)) {
    res <- coef(summary(lm_fits[[j]]))[treatment_pars, ]
    mean[i+j] <- res["Estimate"]
    lb[i+j] <- res["Estimate"] - qnorm(0.975)*res["Std. Error"]
    ub[i+j] <- res["Estimate"] + qnorm(0.975)*res["Std. Error"]
  }
  
  display_df <- data.frame(Models = model_names,
                           `Historical effect` = effect_hst,
                           `Current effect` = effect_cur,
                           `Treatment effect` = "",
                           check.names = FALSE)
  for (i in 1:n_w) {
    w_name <- paste("Weight/Temperature", i)
    display_df[, w_name] <- ""
  }
  reduce(
    seq_along(5:(4+n_w)),
    .init = display_df %>%
              kbl(booktabs = T, caption = caption) %>%
              kable_styling(bootstrap_options = c("striped", "hover"),
                            full_width = FALSE) %>%
              column_spec(4, image = spec_pointrange(
                x = mean, 
                xmin = lb, 
                xmax = ub, 
                vline = effect_cur)
              ),
    .f = function(acc, i) {
      j <- (5:(4+n_w))[i]
      column_spec(
        acc, j,
        image = spec_pointrange(
          x = meanw[, j-4], 
          xmin = lbw[, j-4], 
          xmax = ubw[, j-4], 
          vline = 0.5)
      )
    }
  )
  
}

```

```{r include=FALSE}
## Load model
load("ancova_mixture_prior.RData")
load("ancova_power_prior.RData")
```

<!-- 1. FMP -->

```{r fig.cap="Trace plots of the posterior samples of treatment effect and residual variance under the fixed mixture prior. The shaded region marks burn-in. MCMC ran with 4 chains and 5,000 draws per chain.",eval=FALSE}
## Fixed mixture prior
#data <- build_ancova_data(data_cur, data_hst, I(hba1c - hba1c_baseline) ~ hba1c_baseline + treatment)
#ancova_fmp_fit <- run_model(mixture_model, data, save_warmup = TRUE)
#save(ancova_fmp_fit, file = "ancova_fmp_fit.RData")
load("ancova_fmp_fit.RData")
traceplot(ancova_fmp_fit$fit, pars = c("beta[3]", "sigma2"), inc_warmup = TRUE, nrow = 2) + ggtitle("Trace plot for fixed mixture prior") + theme(plot.title = element_text(hjust = 0.5, size = 14, face = "bold"))
```

```{r fig.cap="Pair plots of the posterior samples of treatment effect and residual variance under the fixed mixture prior. The shaded region marks burn-in. MCMC ran with 4 chains and 5,000 draws per chain.",eval=FALSE}
pairs(ancova_fmp_fit$fit, pars = c("beta[3]", "sigma2"), main = "Pair plot for fixed mixture prior")
```

<!-- 2. RMP -->

```{r fig.cap="Trace plots of the posterior samples of treatment effect, residual variance, and weight under the random mixture prior. The shaded region marks burn-in. MCMC ran with 4 chains and 5,000 draws per chain.",eval=FALSE}
## Random mixture prior
#data <- build_ancova_data(data_cur, data_hst, I(hba1c - hba1c_baseline) ~ hba1c_baseline + treatment, beta_vag = coef(lm(I(hba1c - hba1c_baseline) ~ hba1c_baseline + treatment, data_cur)), estimate_w = TRUE)
#ancova_rmp_fit <- run_model(mixture_model, data, save_warmup = TRUE)
#save(ancova_rmp_fit, file = "ancova_rmp_fit.RData")
load("ancova_rmp_fit.RData")
traceplot(ancova_rmp_fit$fit, pars = c("beta[3]", "sigma2", "w_eff"), inc_warmup = TRUE, nrow = 3) + ggtitle("Trace plot for random mixture prior") + theme(plot.title = element_text(hjust = 0.5, size = 14, face = "bold"))
```

```{r fig.cap="Pair plots of the posterior samples of treatment effect, residual variance, and weight under the random mixture prior. The shaded region marks burn-in. MCMC ran with 4 chains and 5,000 draws per chain.",eval=FALSE}
pairs(ancova_rmp_fit$fit, pars = c("beta[3]", "sigma2", "w_eff"), main = "Pair plot for random mixture prior")
```

<!-- 3. FPP -->

```{r fig.cap="Trace plots of the posterior samples of treatment effect and residual variance under the fixed power prior. The shaded region marks burn-in. MCMC ran with 4 chains and 5,000 draws per chain.",eval=FALSE}
## Fixed power prior
# data <- build_ancova_data(data_cur, data_hst, I(hba1c - hba1c_baseline) ~ hba1c_baseline + treatment)
# ancova_fpp_fit <- run_model(power_model, data, save_warmup = TRUE)
# save(ancova_fpp_fit, file = "ancova_fpp_fit.RData")
load("ancova_fpp_fit.RData")
traceplot(ancova_fpp_fit$fit, pars = c("beta[3]", "sigma2"), inc_warmup = TRUE, nrow = 2) + ggtitle("Trace plot for fixed power prior") + theme(plot.title = element_text(hjust = 0.5, size = 14, face = "bold"))
```

```{r fig.cap="Pair plots of the posterior samples of treatment effect and residual variance under the fixed power prior. The shaded region marks burn-in. MCMC ran with 4 chains and 5,000 draws per chain.",eval=FALSE}
pairs(ancova_fpp_fit$fit, pars = c("beta[3]", "sigma2"), main = "Pair plot for fixed power prior")
```

<!-- 4. UPP -->

```{r fig.cap="Trace plots of the posterior samples of treatment effect, residual variance, and temperature under the unnormalized power prior. The shaded region marks burn-in. MCMC ran with 4 chains and 5,000 draws per chain.",eval=FALSE}
## Random power prior

# data <- build_ancova_data(data_cur, data_hst, I(hba1c - hba1c_baseline) ~ hba1c_baseline + treatment, estimate_w = TRUE, normalization = FALSE)
# ancova_upp_fit <- run_model(power_model, data, save_warmup = TRUE)
# save(ancova_upp_fit, file = "ancova_upp_fit.RData")
load("ancova_upp_fit.RData")
traceplot(ancova_upp_fit$fit, pars = c("beta[3]", "sigma2", "w_eff"), inc_warmup = TRUE, nrow = 3) + ggtitle("Trace plot for unnormalized power prior") + theme(plot.title = element_text(hjust = 0.5, size = 14, face = "bold"))
```

```{r fig.cap="Pair plots of the posterior samples of treatment effect, residual variance, and temperature under the unnormalized power prior. The shaded region marks burn-in. MCMC ran with 4 chains and 5,000 draws per chain.",eval=FALSE}
pairs(ancova_upp_fit$fit, pars = c("beta[3]", "sigma2", "w_eff"), main = "Pair plot for unnormalized power prior")
```

<!-- 5. ANPP -->

```{r fig4-1, fig.show='hold', fig.cap="Trace and pair plots of the posterior samples of treatment effect, residual variance, and temperature under the approximately normalized power prior using path sampling. The shaded region in trace plots marks burn-in. MCMC ran with 4 chains and 5,000 iterations per chain.", out.width=c('48%','48%'), fig.asp=0.7}
## Normalized power prior using path sampling

# data <- build_ancova_data(data_cur, data_hst, I(hba1c - hba1c_baseline) ~ hba1c_baseline + treatment, estimate_w = TRUE, normalization = TRUE, exact_constant = FALSE, wknots = knots$wknots, lgCknots = knots$lgCknots)
# ancova_anpp_fit <- run_model(power_model, data, save_warmup = TRUE)
# save(ancova_anpp_fit, file = "ancova_anpp_fit.RData")
load("ancova_anpp_fit.RData")
traceplot(ancova_anpp_fit$fit, pars = c("beta[3]", "sigma2", "w_eff"), inc_warmup = TRUE, nrow = 3) + ggtitle("Trace plot for approximately normalized power prior") + theme(plot.title = element_text(hjust = 0.5, size = 14, face = "bold"))
pairs(ancova_anpp_fit$fit, pars = c("beta[3]", "sigma2", "w_eff"), main = "Pair plot for approximately normalized power prior")
```

<!-- 6. NPP -->

```{r fig4-2, fig.show="hold", fig.cap="Trace and pair plots of the posterior samples of treatment effect, residual variance, and temperature under the normalized power prior. The shaded region in trace plots marks burn-in. MCMC ran with 4 chains and 5,000 iterations per chain.", out.width=c('48%','48%'), fig.asp=0.7}
## Normalized power prior with exact constant

# data <- build_ancova_data(data_cur, data_hst, I(hba1c - hba1c_baseline) ~ hba1c_baseline + treatment, estimate_w = TRUE, normalization = TRUE, exact_constant = TRUE)
# ancova_npp_fit <- run_model(power_model, data, save_warmup = TRUE)
# save(ancova_npp_fit, file = "ancova_npp_fit.RData")
load("ancova_npp_fit.RData")
traceplot(ancova_npp_fit$fit, pars = c("beta[3]", "sigma2", "w_eff"), inc_warmup = TRUE, nrow = 3) + ggtitle("Trace plot for normalized power prior") + theme(plot.title = element_text(hjust = 0.5, size = 14, face = "bold"))
pairs(ancova_npp_fit$fit, pars = c("beta[3]", "sigma2", "w_eff"), main = "Pair plot for normalized power prior")
```

下表比较了ANPP、NPP和仅使用ANCOVA时的分析结果，可以看到ANPP和NPP相较于ANCOVA有更短的可信区间。

```{r tab4-1}
stan_fits <- list(
                  ancova_anpp_fit,
                  ancova_npp_fit)
lm_fits <- list(lm(I(hba1c - hba1c_baseline) ~ hba1c_baseline + treatment, data_cur))
tabulize(stan_fits, lm_fits,
         model_names = c("ANPP", "NPP", "ANCOVA"),
         treatment_pars = "treatment",
         effect_hst = 0.6,
         effect_cur = 0.6,
         caption = "Overall comparison of results from the Bayesian models and ANCOVA. There is no prior-data conflict in this example. The point estimate and 95% credible/confidence interval for treatment effect and weight/temperature parameter are shown in the last two columns.")
```

下图比较了使用path sampling近似的$\log C(w)$和基于式\@ref(eq:5)的$\log C(w)$。

```{r fig4-3, fig.cap="Comparison of the path sampling approximated normalization constant (red) and the closed-form normalization constant (blue)."}
library(mvtnorm)
w <- (0:200/200)^2
data <- build_ancova_data(data_cur, data_hst, I(hba1c - hba1c_baseline) ~ hba1c_baseline + treatment, estimate_w = TRUE, normalization = TRUE, exact_constant = FALSE, wknots = knots$wknots, lgCknots = knots$lgCknots)
n <- data$n
p <- data$p
beta_hst <- data$beta_hst
beta_vag <- data$beta_vag
Sigma_hst <- tcrossprod(data$L_Sigma_hst)
Sigma_vag <- tcrossprod(data$L_Sigma_vag)
logCfunc <- function (wi) {
  0.5*p*(1-wi)*log(2*pi) + 0.5*(1-wi)*log(det(Sigma_hst)) - 0.5*p*log(wi) + dmvnorm(beta_hst, beta_vag, Sigma_hst/wi+Sigma_vag, log = T)
}
logC <- unlist(lapply(w, logCfunc))
plot(knots$wknots, knots$lgCknots, xlab = "w", ylab = "log C(w)", type = "l", col = "red", lwd = 2, main = "Performance of path sampling")
lines(w, logC, col = "blue", lwd = 2)
legend("topright", legend = c("Path sampling", "Closed form"), col = c("red", "blue"), lty = 1, lwd = 2)
```

#### 统计性能比较

本节比较prior-data conflict的程度变化时各方法的统计性能，历史数据的治疗效应为0.6，当前研究的治疗效应（current effect size）从0增加至0.6。每个current effect size下的模拟次数为1,000次。

图\@ref(fig:fig4-4)展示了FMP ($w=0.5$), RMP, FPP ($w=0.5$), UPP, ANPP, NPP, ANCOVA七种方法的bias和RMSE随current effect size的变化。图\@ref(fig:fig4-5)展示了各方法的probability of rejection随current effect size的变化，其中，当$\beta=0$时，probability of rejection即为type I error。对于probability of rejection的计算，ANCOVA方法使用单侧t-检验来检验$H_0:\beta=0\text{ v.s. }H_1:\beta>0$，其他的贝叶斯方法使用10,000次MCMC抽样的后验概率$P(\beta\gt 0|D, D_{hst})\gt 0.975$来检验。注意到由于模拟次数为1,000次，$\alpha=0.025$下type I error的95%可能范围约为0.015至0.035，power的95%可能范围约为0.775至0.825。对于各方法统计性能的比较主要有以下结果：

1. 所有信息借用的方法在无prior-data conflict下bias与RMSE最低；
2. FMP和RMP的表现几乎相同，验证了Methods部分对混合先验的讨论，这两种方法的RMSE在弱prior-data conflict ($\beta$在0.45~0.6之间) 的场景下表现最佳；
2. FPP的bias随current effect size的变化较其他方法更不稳定，且type I error最高；
3. UPP的bias比ANCOVA外的其他方法更低，假设检验更保守;
4. ANPP和NPP的表现几乎相同，表明path sampling的近似对后验推断几乎没有影响，这两种方法的表现也与FMP和FPP相近；
5. 由于ANCOVA方法不借用任何信息，其bias均在0附近，但是RMSE在弱prior-data conflict场景下要高于其他信息借用方法，而probability of rejection则符合预设的$\alpha$和power。

```{r fig4-4, fig.show="hold", fig.width=9, fig.cap="Bias and RMSE of the compared methods under various current effect size. The historical effect size is $\\tau=0.6$, which is indicated by the vertical red dashed line. The strength of prior-data conflict reduces as current effect size $\\beta$ increases from 0 to 0.6. Bias and RMSE are estimated based on 1,000 simulation datasets.", out.width=c('48%','48%'), fig.asp=0.7}
load("simres1.RData")
p_bias
p_rmse
```

```{r fig4-5, fig.width=9, fig.cap="Probability of rejection of the compared methods under various current effect size. The historical effect size is $\\tau=0.6$, which is indicated by the vertical red dashed line. The strength of prior-data conflict reduces as current effect size $\\beta$ increases from 0 to 0.6. Note that when $\\beta=0$, the probability of rejection is actually the type I error. Reference lines indicate the nominal significance level $\\alpha=0.025$ and the pre-defined power of 0.8 when $\\beta=0.6$. For ANCOVA, the null hypothesis $H_0:\\beta=0$ is tested with right-tailed t-test at the level of 0.025, while for Bayesian methods, the null hypothesis is tested by $P(\\beta\\gt 0|D, D_{hst})\\gt 0.975$ with 10,000 MCMC iterations. Probability of rejection is estimated based on 1,000 simulation datasets."}
p_power
```

表\@ref(tab:tab4-2)展示了上述方法的bias, RMSE, CI length, coverage probability, probability of rejection和$\hat{R}$在1,000次模拟试验中的具体数值，其中$\hat{R}$是用于检查MCMC抽样是否收敛的统计量，$\hat{R}>1.01$表明MCMC抽样的收敛可能存在问题。

就各方法的95%置信或可信区间而言，贝叶斯信息借用的方法在无或弱prior-data conflict场景下，比ANCOVA有更高的coverage probability和更窄的CI length。在强prior-data conflict下，尽管贝叶斯信息借用的方法有更窄的CI length，它们的coverage probability均低于95%。

```{r tab4-2}
summary_disp %>%
  mutate("Historical effect size" = 0.6, .before = 2) %>%
  rename("Current effect size" = "beta",
         "Method" = "method",
         "Coverage probability" = "Coverage",
         "CI length" = "CI_length") %>%
  kbl(format = "html", digits = 3, booktabs = T, centering = T,
      caption = "Statistical performance of the compared methods under various current effect size. Confidence/Credible intervals are derived at the level of 95%. For ANCOVA, the null hypothesis $H_0:\\beta=0$ is tested with right-tailed t-test at the level of 0.025, while for Bayesian methods, the null hypothesis is tested by $P(\\beta\\gt 0|D, D_{hst})\\gt 0.975$ with 10,000 MCMC iterations. All metrics are estimated based on 1,000 simulation datasets.") %>%
  kable_styling(bootstrap_options = c("striped", "hover"), full_width = FALSE) %>%
  collapse_rows(columns = 1, valign = "top")
```


#### 试验设计阶段的先验参数选取 {#sec4.1.1.3}

本节的模拟试验旨在探索试验设计阶段，如何从统计模拟的角度给出合适范围的$w$。

历史数据的治疗效应$\tau=0.6$。试验设计阶段，若假设治疗效应$\beta=0.6$，主要终点的样本方差为$Var(Y)=0.84$，协变量包括治疗分组和基线水平，单侧检验$\alpha=0.025$，由于入组困难，power为0.7时，样本量应为44。此时，考虑以下两个场景：

* 场景一：在原假设下，$\beta=0$时type I error随$w$的变化，此时为强prior-data conflict；
* 场景二：在试验设计假设下，$\beta=0.6$时power随$w$的变化，此时无prior-data conflict。

图\@ref(fig:fig4-6)展示了type I error和power随$w$的变化，左图为fixed mixture prior方法，右图为fixed power prior方法：

1. 当$w=0$时，两种方法均不借用历史数据，因此type I error被控制在$\alpha=0.025$，power接近试验设计的0.7；
2. 即使$w$的取值较小，例如$w=0.1$时，两种方法的type I error也会膨胀。

在实际应用中，对于$w$的选取可能需考虑到可容忍的type I error和目标power。在本模拟场景中：

1. 若可容忍的type I error是$0.025$（也即认为疗效有可能为0，对疗效没有把握），那么相应的$w$取值接近$0$，几乎没有借用历史数据，因此power几乎不会提升；
2. 若可容忍的type I error是$0.1$（也即认为疗效为0的可能性较低，对疗效有把握），那么相应的$w$取值范围约为$[0, 0.2)$，此时，若目标power为0.8，那么$w$的取值在$0.1$附近较为合适。

可容忍的type I error的选择或许可通过由医学给定$P(H_0\text{ is true})$和$P(H_1\text{ is true})$，约束posterior probability of type I error $P(H_0\text{ is true}|\text{Rejection})\leq\pi$来确定。例如，在上述模拟场景中，若$P(H_0\text{ is true})=0.63, P(H_1\text{ is true})=0.37, \pi=0.05$，则可接受的type I error为$0.025$；$P(H_0\text{ is true})=0.3, P(H_1\text{ is true})=0.7, \pi=0.05$，则可接受的type I error为$0.098\approx0.1$。

```{r fig4-6, fig.width=9, fig.asp=0.65, fig.cap="Type I error (current effect size=0.00, red solid lines with round points) and power (current effect size=0.60, blue dashed lines with triangle points) for FMP (left) and FPP (right) against $w$. Reference lines are drawn at $0.025, 0.05, 0.1, 0.7, \\text{ and } 0.8$, ordered bottom-to-top. The null hypothesis is tested by $P(\\beta\\gt 0|D, D_{hst})\\gt 0.975$ with 10,000 MCMC iterations. Probability of rejection is estimated based on 1,000 simulation datasets."}
load("simres2.RData")
p_power
```

图\@ref(fig:fig4-7)展示了DIC随$w$的变化：

1. 在无prior-data conflict时（$\beta=0.6,\tau=0.6$），对于FMP和FPP方法，DIC的最小值点均为1，此时DIC建议借用全部历史数据；
2. 在强prior-data conflict时（$\beta=0,\tau=0.6$），对于FMP方法，DIC的最小值点在$[0, 0.1)$，对于FPP方法，DIC的最小值点在$(0, 0.2)$。

综合基于type I error和power选择$w$的结果，当不确定$w$的取值时，基于DIC选择$w$可能是一个可行方案，提供了不同角度支持$w$选取的合理性。

```{r fig4-7, fig.width=9, fig.asp=0.65, fig.cap="Mean DIC for FMP (left) and FPP (right) against $w$ when there is a strong prior-data conflict (current effect size=0.00, red solid lines with round points) or no prior-data conflict (current effect size=0.60, blue dashed lines with triangle points)."}
p_dic
```


#### 数据分析阶段的敏感性分析

本节的模拟试验旨在探索数据分析阶段，如何对$w$的不同取值，进行敏感性分析。

历史数据的治疗效应$\tau=0.6$。考虑以下两个场景：

* 场景一：strong prior-data conflict，此时$\beta=0$，样本量等其他参数与第\@ref(sec4.1.1.3)节相同；
* 场景二：moderate prior-data conflict，此时$\beta=0.3$，样本量等其他参数与第\@ref(sec4.1.1.3)节相同。

图\@ref(fig:fig4-8)和\@ref(fig:fig4-9)分别展示了场景一和场景二中，两种方法的区间估计随$w$的变化情况，左图为fixed mixture prior方法，右图为fixed power prior方法：

```{r}

```


<!-- # Backup Code -->

```{r include = FALSE}
# library(nimble)
# dppancova <- nimbleFunction(
#   run = function(x = double(1),
#                  y_hst = double(1),
#                  X_hst = double(2),
#                  w = double(0),
#                  beta0 = double(1),
#                  Sigma0 = double(2),
#                  a0 = double(0),
#                  b0 = double(0),
#                  log = integer(0, default = 0)) {
#     returnType(double(0))
#     
#     n <- dim(X_hst)[1]
#     p <- dim(X_hst)[2]
#     
#     beta <- x[1:p]
#     sigma2 <- x[p+1]
#     
#     logProb <- w*sum(dnorm(y_hst, mean = X_hst %*% beta, sd = sqrt(sigma2), log = TRUE)) +
#       dmnorm_chol(beta, mean = beta0, cholesky = sqrt(n*sigma2)*chol(Sigma0), log = TRUE) + 
#       dinvgamma(sigma2, shape = a0, scale = b0, log = TRUE)
#     if(log) return(logProb)
#     else return(exp(logProb))
# })
# 
# mpCode <- nimbleCode({
#   if (estimate_w) {
#     w ~ dunif(0, 1)
#   } else {
#     w <- w_fixed
#   }
# 
#   ## Power prior
#   theta[1:(p+1)] ~ dppancova(y_hst = y0[1:n0], 
#                              X_hst = X0[1:n0, 1:p],
#                              w = w,
#                              beta0 = beta0[1:p],
#                              Sigma0 = Sigma0[1:p, 1:p],
#                              a0 = a0,
#                              b0 = b0)
#   beta[1:p] <- theta[1:p]
#   sigma2 <- theta[p+1]
# 
#   ## Likelihood
#   y[1:n] ~ dmnorm(mu[1:n], cov = Cov_y[1:n, 1:n])
#   mu[1:n] <- X[1:n, 1:p] %*% beta[1:p]
#   Cov_y[1:n, 1:n] <- sigma2*diag(n)
# })
# 
# mk_design <- function(df, formula) {
#   X <- model.matrix(formula, df)
#   y <- model.response(model.frame(formula, df))
#   list(y = as.numeric(y), X = X, cols = colnames(X))
# }
# 
# build_nimble_consts <- function(df_cur, df_hst, formula,
#                                w_fixed = 0.5, estimate_w = FALSE) {
#   des_cur <- mk_design(df_cur, formula)
#   des_hst <- mk_design(df_hst, formula)
#   if (estimate_w) {
#     list(n = nrow(des_cur$X),
#          n0 = nrow(des_hst$X),
#          p = ncol(des_cur$X),
#          estimate_w = estimate_w)
#   } else {
#     list(n = nrow(des_cur$X),
#          n0 = nrow(des_hst$X),
#          p = ncol(des_cur$X),
#          w_fixed = w_fixed,
#          estimate_w = estimate_w)
#   }
# }
# 
# build_nimble_data <- function(df_cur, df_hst, formula,
#                               beta0 = NULL, Sigma0 = NULL, a0 = NULL, b0 = NULL) {
#   des_cur <- mk_design(df_cur, formula)
#   des_hst <- mk_design(df_hst, formula)
#   fit_cur <- lm(formula, df_cur)
#   fit_hst <- lm(formula, df_hst)
# 
#   if (is.null(beta0)) {
#     beta0 <- coef(fit_hst)
#   }
#   if (is.null(Sigma0)) {
#     Sigma0 <- solve(crossprod(des_hst$X))
#   }
#   if (is.null(a0)) {
#     a0 <- 1/2
#   }
#   if (is.null(b0)) {
#     b0 <- 1/2*sum(fit_hst$residuals^2)
#   }
# 
#   list(
#     #n = nrow(des_cur$X),
#     y = des_cur$y,
#     X = des_cur$X,
#     #n0 = nrow(des_hst$X),
#     y0 = des_hst$y,
#     X0 = des_hst$X,
#     beta0 = beta0,
#     beta1 = coef(fit_cur),
#     Sigma0 = Sigma0,
#     Sigmav = solve(crossprod(des_cur$X)),
#     sigma2h = sigma(fit_hst)^2,
#     sigma2v = sigma(fit_cur)^2,
#     RSS = 2*b0,
#     a0 = a0,
#     b0 = b0
#   )
# }
# build_nimble_inits <- function(df_cur, formula, estimate_w = FALSE) {
#   des_cur <- mk_design(df_cur, formula)
#   fit_cur <- lm(formula, df_cur)
#   if (estimate_w) {
#     list(betah = coef(fit_cur),
#          lsigma2 = log(sigma(fit_cur)^2),
#          sigma2h = sigma(fit_cur)^2,
#          sigma2v = nrow(des_cur$X)*sigma(fit_cur)^2,
#          theta = c(coef(fit_cur), sigma(fit_cur)^2),
#          z = 1,
#          w = 1)
#   } else {
#     list(betah = coef(fit_cur),
#          lsigma2 = log(sigma(fit_cur)^2),
#          sigma2h = sigma(fit_cur)^2,
#          sigma2v = nrow(des_cur$X)*sigma(fit_cur)^2,
#          theta = c(coef(fit_cur), sigma(fit_cur)^2),
#          z = 1)
#   }
# }
# 
# 
# # --- Example run ---
# n <- 300
# n_hst <- 200
# n_cur <- n - n_hst
# hst <- c(rep(1, n_hst), rep(0, n_cur))
# alpha <- 0.5
# tau <- -5
# delta <- 0
# gamma <- 0.01
# treatment <- rbinom(n, 1, 0.5)
# hba1c_baseline <- rnorm(n, 45, 2)
# e <- rnorm(n, sd = 2)
# hba1c <- hba1c_baseline + alpha + (tau + delta*hst)*treatment + gamma*hba1c_baseline + e
# data <- data.frame(hba1c_baseline, treatment, hst, hba1c)
# data_hst <- subset(data, hst == 1)
# data_cur <- subset(data, hst == 0)
# mpConsts <- build_nimble_consts(data_cur, data_hst, I(hba1c - hba1c_baseline) ~ hba1c_baseline + treatment, estimate_w = TRUE)
# mpData <- build_nimble_data(data_cur, data_hst, I(hba1c - hba1c_baseline) ~ hba1c_baseline + treatment)
# mpInits <- build_nimble_inits(data_cur, I(hba1c - hba1c_baseline) ~ hba1c_baseline + treatment, estimate_w = TRUE)
# ancova_mp <- nimbleModel(code = mpCode, name = "ancova_mp", constants = mpConsts,
# data = mpData, inits = mpInits)
# 
# cancova_mp <- compileNimble(ancova_mp, showCompilerOutput = T)
# mcmcConf <- configureMCMC(ancova_mp, monitors = c('beta','w'))
# mcmc <- buildMCMC(mcmcConf)
# cMcmc <- compileNimble(mcmc, project = ancova_mp)
# samples1 <- runMCMC(cMcmc, niter = 5000, nburnin = 2500, nchains = 4, summary = TRUE)
# ancova_mp$setData(mpData)
# cancova_mp$setData(mpData)
# ancova_mp$setInits(mpInits)
# cancova_mp$setInits(mpInits)
# 
# samples2 <- runMCMC(cMcmc, niter = 2500, nburnin = 0, nchains = 4, summary = TRUE)
# 
# 
# 
# 
# mcmc.out <- nimbleMCMC(code = mpCode, constants = mpConsts,
#                        data = mpData, inits = mpInits,
#                        nchains = 4, niter = 5000, nburnin = 2500,
#                        summary = TRUE,
#                        monitors = c('beta','w'))
```

# Reference {#sec5}